# OpenAI API key for LLM-based translation (required for remote engine)
OPENAI_API_KEY=your-key-here

# Optional: override the default remote model
# OPENAI_MODEL=gpt-4o-mini

# Local LLM server (used with --local flag)
OPENAI_BASE_URL=http://localhost:8080/v1
LOCAL_LLM_MODEL=LFM2.5-8B-A1B-Q8_0
